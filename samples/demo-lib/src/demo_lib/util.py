"""Вспомогательные функции. Одна из них намеренно без описания."""


def helper(value: int) -> int:
    return value + 1


def _hidden(value: int) -> int:
    """Служебная функция не попадает в справочник."""
    return value

"""Короткое экранирование подписей, которые попадают в Markdown."""


def md_label(value: str) -> str:
    """Оставляет подчёркивания в именах вроде ``__init__`` видимыми."""
    return value.replace("\\", "\\\\").replace("*", "\\*").replace("_", "\\_")
